## 4.1

### 4.1.1

Look BoundedBuffer.java

### 4.1.2
The class is thread-safe because only one thread can edit the critical section at a time. The queue is behind a semaphore 1, which acts as a normal lock and garuantees mutual exclusion.
Furthermore, the other semaphores garuantee that take and insert will wait until there action has space available. No escaping is possible since no references are leaked. Conlusion, no data races are present meaning it is thread-safe.

### 4.1.3
Most likely not. Barriers usually require that threads does the same action. If it should be implemented here one would be doing take and another insert. Also this would remove the ability to insert independtly.

### 4.2

### 4.2.1

Look Person.java

### 4.2.2

The class is thread safe because there are no data races present. All get functions and set functions are synchronized on the instance. The id's are generated by a lock on the class instead.The clas only exposes the values through the get functions.

### 4.2.3

Look PersonThreadTest.java

### 4.2.4

No, we did not test it with a suefficient amount of threads and people.
